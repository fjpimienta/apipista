type Mutation {
  registerTeacher(teacher: TeacherInput!): ResultTeacher
  updateTeacher(teacher: TeacherInput!): ResultTeacher
  deleteTeacher(id: ID!): ResultTeacher
  blockTeacher(id: ID!, unblock: Boolean = false): ResultTeacher
}
