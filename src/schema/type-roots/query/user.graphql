type Query {
  users(
    page: Int = 1,
    itemsPage: Int = 10,
    active: ActiveFilterEnum = ACTIVE,
    filterName: String = "",
    role: Role = CLIENT
  ): ResultUsers
  user(id: ID!): ResultUser
  userId: String
}
