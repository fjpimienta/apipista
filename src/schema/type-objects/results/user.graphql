type ResultUser implements Result {
  status: Boolean!
  message: String!
  user: User
}

type ResultUsers implements Result {
  status: Boolean!
  message: String!
  users: [User!]
  info: ResultInfo
}
