type ResultUser implements Result {
    status: Boolean!
    message: String!
    user: User
}

type ResultUsers implements Result {
    status: Boolean!
    message: String!
    users: [User!]
    info: ResultInfo
}

type ResultInfo {
    page: Int!
    pages: Int!
    total: Int!
    itemsPage: Int!
}
